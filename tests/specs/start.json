[
  {
    "name": "/start begins onboarding with quiet-hour defaults",
    "steps": [
      {
        "send": { "text": "/start" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "Your quiet hours are set to 00:00–08:00 UTC. Choose whether to receive a morning summary." }
          }
        ]
      }
    ]
  }
]
